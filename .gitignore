# Secrets — never commit
.env

# User documents — never commit
source_files/

# Generated outputs
outputs/

# Old dense matrix (replaced by .npz)
rag-document-search/references/tfidf_matrix.npy

# Embedding matrix — committed so Railway gets it (16 MB)
# Rebuild locally with "Build Embeddings" then commit when corpus changes.

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/

# PyInstaller build artifacts
build/
dist/
*.spec

# macOS
.DS_Store
